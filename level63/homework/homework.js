// The this keyword in programming refers to the current instance of an object or class.
//  It is used to refer to instance variables and methods within the same class.
//  In object-oriented languages like JavaScript, Python (using self), and Java, this helps in distinguishing
//  between local variables and instance properties, particularly in methods. In JavaScript, this can refer to different
//  objects depending on the context of how the method is invoked.

// # Computer class
// class Computer:
//     def __init__(self, brand, processor, ram):
//         self.brand = brand
//         self.processor = processor
//         self.ram = ram

// # Keyboard class
// class Keyboard:
//     def __init__(self, brand, type, layout):
//         self.brand = brand
//         self.type = type
//         self.layout = layout

// # Bus class
// class Bus:
//     def __init__(self, company, capacity, route):
//         self.company = company
//         self.capacity = capacity
//         self.route = route

// # Create objects
// computer = Computer("Dell", "Intel i7", "16GB")
// keyboard = Keyboard("Logitech", "Mechanical", "QWERTY")
// bus = Bus("Greyhound", 50, "NYC to Boston")

// # Print details
// print(f"Computer: {computer.brand}, {computer.processor}, {computer.ram}")
// print(f"Keyboard: {keyboard.brand}, {keyboard.type}, {keyboard.layout}")
// print(f"Bus: {bus.company}, {bus.capacity}, {bus.route}")

